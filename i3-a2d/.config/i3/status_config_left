# UTF-8 : ß

general {
        colors = true
        interval = 1
}

order += "disk /"
order += "disk /datas"
order += "ethernet _first_"
order += "volume master"

volume master {
        format = " %volume"
        format_muted = ""
        device = "pulse"
}

ethernet _first_ {
        format_up = " %ip"
        format_down = ""
}

disk / {
        format = " / %free"
}

disk /datas {
        format = " /datas %free"
}
