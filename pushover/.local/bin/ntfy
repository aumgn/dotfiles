#! /bin/bash

title=$1
shift
msg="$*"

curl -s \
  -F "token=$(cat $HOME/.config/pushover/token)" \
  -F "user=$(cat $HOME/.config/pushover/user)" \
  -F "title=$title" \
  -F "message=$msg" \
  https://api.pushover.net/1/messages.json |
  grep '"status":1' 1>/dev/null 2>&1
